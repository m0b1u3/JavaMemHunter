# v0.6 实现计划 — Tomcat Filter 安全清理

| 项目 | 内容 |
|---|---|
| 设计文档 | `docs/superpowers/specs/2026-05-22-v0.6-tomcat-filter-clean-design.md` |
| 分支 | `feature/v0.6-tomcat-filter-clean` |
| 起始 commit | `c0e2df7` (v0.6 design spec) |
| 任务数 | 12 |
| 节奏 | 每个 task 1 subagent + TDD + 1 commit，沿用 v0.4/v0.5 |

---

## Task 1: CleanPlan / CleanResult / FilterBackup 数据模型（TDD）

**Files:**
- New: `agent/src/main/java/com/memhunter/agent/model/CleanPlan.java`
- New: `agent/src/main/java/com/memhunter/agent/model/CleanResult.java`
- New: `agent/src/main/java/com/memhunter/agent/model/FilterBackup.java`
- New: `agent/src/test/java/com/memhunter/agent/model/CleanPlanTest.java`
- New: `agent/src/test/java/com/memhunter/agent/model/CleanResultTest.java`

**Steps:**
- [ ] 编写 CleanPlanTest：Jackson round-trip（writeValueAsString + readValue），断言所有字段保留
- [ ] 编写 CleanResultTest：同上 + verifiedDisappeared 默认 false
- [ ] 实现 3 个数据类（字段按设计文档 §3.1/§3.2/§3.4）
- [ ] FilterBackup 不需要序列化测试（仅内存对象）
- [ ] `mvn -pl agent test` 通过

**Commit:** `feat(agent): add CleanPlan/CleanResult/FilterBackup data models`

---

## Task 2: AgentArgs 扩展 + 互斥校验

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/AgentArgs.java`
- New: `agent/src/test/java/com/memhunter/agent/AgentArgsCleanTest.java`

**Steps:**
- [ ] AgentArgsCleanTest：
  - `clean --id X --dry-run` 解析正确
  - `clean --id X --confirm` 解析正确
  - `clean --id X` 缺 dry-run/confirm → IllegalArgumentException
  - `clean --id X --dry-run --confirm` 两者同时 → IllegalArgumentException
  - `clean --id X --force` 缺 confirm → IllegalArgumentException
  - `clean --dry-run` 缺 id → IllegalArgumentException
  - `verify --id X` 解析正确
- [ ] KNOWN_OPTIONS 追加 id/dry-run/confirm/force/evidence-dir
- [ ] parse() 后追加 validate(command) 钩子做互斥校验

**Commit:** `feat(agent): extend AgentArgs to support clean/verify commands`

---

## Task 3: EvidenceWriter（TDD）

**Files:**
- New: `agent/src/main/java/com/memhunter/agent/cleaner/EvidenceWriter.java`
- New: `agent/src/main/java/com/memhunter/agent/cleaner/CleanPlanReader.java`
- New: `agent/src/test/java/com/memhunter/agent/cleaner/EvidenceWriterTest.java`
- New: `agent/src/test/java/com/memhunter/agent/cleaner/CleanPlanReaderTest.java`

**Steps:**
- [ ] EvidenceWriterTest：给定 tmp dir + Finding + CleanPlan + bytecode byte[]
  - 断言落盘 5 个文件：finding.json / clean-plan.json / before-snapshot.json / bytecode.bin / bytecode.sha256
  - bytecode.sha256 与 bytecode.bin 内容一致
  - clean-result.json 由独立方法 writeResult() 写出
- [ ] CleanPlanReaderTest：写入 → 读回 round-trip
- [ ] EvidenceWriter 实现（Jackson + DigestUtils）

**Commit:** `feat(agent): add EvidenceWriter and CleanPlanReader`

---

## Task 4: TomcatFilterCleaner Phase A/B（复扫 + 备份）

**Files:**
- New: `agent/src/main/java/com/memhunter/agent/cleaner/Cleaner.java`（接口）
- New: `agent/src/main/java/com/memhunter/agent/cleaner/TomcatFilterCleaner.java`（Phase A+B）
- New: `agent/src/test/java/com/memhunter/agent/cleaner/TomcatFilterCleanerPhaseABTest.java`

**Steps:**
- [ ] 定义 Cleaner 接口：`CleanResult clean(Finding f, ScanContext ctx, Path evidenceDir, boolean forced)`
- [ ] Phase A：复扫 → 定位 Finding → 校验 type/score
- [ ] Phase B：通过反射读 filterDefs/filterMaps.array/filterConfigs，构造 FilterBackup
- [ ] 测试用 fake StandardContext-like 对象（含三个字段），断言 FilterBackup 完整性
- [ ] 测试覆盖：finding 不存在 / type 不匹配 / score < 7 无 force → 全部返回 CleanResult{success=false}

**Commit:** `feat(agent): TomcatFilterCleaner Phase A/B - locate + backup`

---

## Task 5: TomcatFilterCleaner Phase C + RollbackManager（核心算法）

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/cleaner/TomcatFilterCleaner.java`
- New: `agent/src/main/java/com/memhunter/agent/cleaner/RollbackManager.java`
- New: `agent/src/main/java/com/memhunter/agent/cleaner/RollbackFailedException.java`
- New: `agent/src/test/java/com/memhunter/agent/cleaner/TomcatFilterCleanerPhaseCTest.java`
- New: `agent/src/test/java/com/memhunter/agent/cleaner/RollbackManagerTest.java`

**Steps:**
- [ ] Phase C：原子副本替换，严格顺序 filterConfigs → filterMaps → filterDefs
- [ ] 每一步通过反射 setField 写回；任一步异常立即抛 → 触发 Phase F
- [ ] RollbackManager.restore() 按反向顺序 filterDefs → filterMaps → filterConfigs 回写
- [ ] PhaseCTest：fake StandardContext 验证三个字段都被替换为新副本，且不含目标
- [ ] RollbackManagerTest：在 Phase C 中段注入异常 → 验证全部恢复

**Commit:** `feat(agent): TomcatFilterCleaner Phase C atomic copy-replace + RollbackManager`

---

## Task 6: TomcatFilterCleaner Phase D/E（destroy + verify）

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/cleaner/TomcatFilterCleaner.java`
- New: `agent/src/test/java/com/memhunter/agent/cleaner/TomcatFilterCleanerFullFlowTest.java`

**Steps:**
- [ ] Phase D：反射调用 backup.originalFilterConfig.release()，try-catch 吞 destroy 异常
- [ ] Phase E：再次跑 TomcatFilterScanner，检查 targetId 是否消失
  - 仍存在 → rollback + CleanResult{success=false, verifiedDisappeared=false}
  - 消失 → CleanResult{success=true, verifiedDisappeared=true}
- [ ] FullFlowTest：fake context + fake scanner，跑通 A→E 全流程

**Commit:** `feat(agent): TomcatFilterCleaner Phase D/E - destroy + verify`

---

## Task 7: CleanInteractor（attach 端 stdin yes 读取）

**Files:**
- New: `attach/src/main/java/com/memhunter/attach/CleanInteractor.java`
- New: `attach/src/test/java/com/memhunter/attach/CleanInteractorTest.java`

**Steps:**
- [ ] CleanInteractor.promptYes(InputStream, PrintStream, CleanPlan) → boolean
  - 打印 CleanPlan 摘要
  - 读一行 → 严格 equals "yes" 返回 true，否则 false
- [ ] CleanInteractorTest 用 ByteArrayInputStream 注入 "yes"/"no"/"YES"/"y"/空 → 验证返回值

**Commit:** `feat(attach): add CleanInteractor for stdin yes confirmation`

---

## Task 8: MemHunterAgent dispatch clean / verify

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/MemHunterAgent.java`
- New: `agent/src/main/java/com/memhunter/agent/verify/VerifyExecutor.java`
- New: `agent/src/test/java/com/memhunter/agent/verify/VerifyExecutorTest.java`

**Steps:**
- [ ] agentmain 根据 args.command 分发：`scan` / `clean` / `verify`
- [ ] `clean --dry-run`：执行 Phase A+B，落盘 evidence，不进入 C/D/E
- [ ] `clean --confirm`：读 clean-plan.json，执行 A→E（A 中重新复扫确认）
- [ ] `verify`：跑 TomcatFilterScanner，写 verify-result.json
- [ ] VerifyExecutorTest：fake scanner，验证 stillPresent 字段

**Commit:** `feat(agent): dispatch clean and verify commands in agentmain`

---

## Task 9: AttachMain 接入 clean / verify

**Files:**
- Modify: `attach/src/main/java/com/memhunter/attach/AttachMain.java`
- Modify: `attach/src/main/java/com/memhunter/attach/AttachExecutor.java`

**Steps:**
- [ ] AttachMain 识别 clean / verify 命令
- [ ] `clean --confirm` 流程：
  1. 读 evidence/<id>/clean-plan.json（不存在 → 提示先 dry-run）
  2. 调 CleanInteractor.promptYes → false 则退出 code 1
  3. loadAgent(agent.jar, "clean --id X --confirm [--force]")
  4. 读 evidence/<id>/clean-result.json，打印结果摘要
- [ ] `verify` 流程直接 loadAgent，读 verify-result.json

**Commit:** `feat(attach): wire clean/verify commands with stdin confirmation`

---

## Task 10: 端到端 E2E

**Files:**
- New: `docs/superpowers/specs/v0.6-clean-flow-evidence/`（落盘样例）
- 可能：`test-target-injector/` 已有 FakeFilterInjector，无需修改

**Steps:**
- [ ] 启动 test-target（带 FakeFilter 注入）
- [ ] `scan --output before.json` → 确认 FakeFilter score ≥ 25
- [ ] `clean --id <fakeFilterId> --dry-run --output ./evidence` → 验证 5 个 evidence 文件
- [ ] `clean --id <fakeFilterId> --confirm` 输入 "no" → 验证无副作用
- [ ] 重新 dry-run，再 confirm 输入 "yes" → 验证 clean-result.json.success=true
- [ ] `scan --output after.json` → 确认 FakeFilter 消失
- [ ] `verify --id <fakeFilterId>` → stillPresent=false
- [ ] curl 触发请求 → 业务正常
- [ ] 把 before.json / clean-plan.json / clean-result.json / after.json 归档到 docs/superpowers/specs/v0.6-clean-flow-evidence/

**Commit:** `test: v0.6 end-to-end clean flow verified on FakeFilter`

---

## Task 11: README + 设计文档更新

**Files:**
- Modify: `README.md`
- Modify: `java_memshell_scanner_design.md`（§25 v0.6 标记完成）

**Steps:**
- [ ] README 新增 v0.6 章节：clean / verify 命令用法 + force 说明 + evidence 目录说明
- [ ] 设计文档 §25 v0.6 状态改为 "已完成"
- [ ] 重要警示：生产环境清理后建议重启服务（§21.1）

**Commit:** `docs: update README and design doc for v0.6 clean flow`

---

## Task 12: Final 整体代码评审

**Files:** N/A（评审）

**Steps:**
- [ ] 用 code-reviewer subagent 审 Task 1-11 全部 diff
- [ ] 重点关注：反射字段写入顺序、rollback 完备性、stdin yes 严格匹配、evidence 目录权限
- [ ] 输出 review report，必要时补丁

**Commit:** （视 review 结论决定）

---

## 合并流程（v0.4/v0.5 同款）

完成 Task 1-12 后：

1. `git checkout master && git merge --no-ff feature/v0.6-tomcat-filter-clean`
2. `git push origin master`
3. 在 GitHub Release 标记 v0.6

---

## 风险提醒

- **Task 5 是单点风险最大的 task**：原子副本替换 + 反射 setField 顺序错了会导致 Tomcat 内部状态不一致。务必先把 RollbackManagerTest 写到位再做 Phase C。
- **Task 10 E2E 必须在 fresh JVM 跑**：清理后类残留会影响第二次 attach（§21.2），测试间需要重启 test-target。
