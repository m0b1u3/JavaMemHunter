# v0.13 伪装框架包内存马检测 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让伪装成受信框架包（`org.apache.coyote.*` 等）且 codeSource 为空的内存马（哥斯拉为典型）从 high/suspicious 升到 critical，零误报。

**Architecture:** 纯评分层改动。(1) `WhitelistHitRule` 加 codeSource 可信度门——仅 codeSource 非空时才减 -5。(2) 新增 `MasqueradedPackageRule`——受信前缀 + codeSource 空 → +5。两规则均用 `ctx.whitelist.isFrameworkPackage`，逻辑互斥。装配进 RuleEngine。

**Tech Stack:** Java 8，JUnit 5，Maven（`./mvnw.cmd`），agent 模块。

参考 spec：`docs/superpowers/specs/2026-06-04-v0.13-masquerade-detection-design.md`

**关键既有事实（已确认）：**
- `Whitelist.isFrameworkPackage(String)` 是 public，className==null 时返回 false。受信前缀含 `org.apache.coyote.`/`catalina.`/`tomcat.`/`juli.` 等（`DefaultWhitelist`）。
- `WhitelistHitRule.evaluate` 现为：`if (finding==null||ctx==null||ctx.whitelist==null) return 0; if (ctx.whitelist.isFrameworkPackage(finding.className)) return -5; return 0;`
- `ScanContext` 3-arg 构造：`new ScanContext(null, Whitelist.defaults(), false)`。
- `RuleEngine.defaultRules()` 是 `Arrays.asList(...)`，末项当前是 `new BenignComponentRule()`。
- `RuleEngine.mapLevel`：score≥10 critical，≥7 high，≥4 suspicious。
- `RuleEngine.evaluateOne` 对 `type` 以 `agent-` 开头的 finding 早退、不跑规则链。
- `Finding` 字段：`type`、`className`、`codeSource`、`score`、`level`、`attributes`（Map）。
- **现有 `WhitelistHitRuleTest.hits_framework_package` 传 codeSource=null 期望 -5——本计划会改它。**

---

## Task 1: WhitelistHitRule 加 codeSource 可信度门（TDD）

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/scoring/rules/WhitelistHitRule.java`
- Modify: `agent/src/test/java/com/memhunter/agent/scoring/rules/WhitelistHitRuleTest.java`

- [ ] **Step 1: 改测试到目标行为（先让它失败）**

把 `WhitelistHitRuleTest.java` 整体替换为：

```java
package com.memhunter.agent.scoring.rules;

import com.memhunter.agent.model.Finding;
import com.memhunter.agent.model.ScanContext;
import com.memhunter.agent.scoring.Whitelist;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class WhitelistHitRuleTest {
    private final WhitelistHitRule rule = new WhitelistHitRule();
    private final ScanContext ctx = new ScanContext(null, Whitelist.defaults(), false);

    @Test
    void framework_package_with_codesource_is_trusted_minus5() {
        // Real framework class loaded from a jar → trusted → -5
        assertEquals(-5, rule.evaluate(
                finding("org.apache.tomcat.websocket.server.WsFilter",
                        "file:/opt/tomcat/lib/tomcat-websocket.jar"), ctx));
    }

    @Test
    void framework_package_with_null_codesource_is_not_trusted_zero() {
        // v0.13: framework prefix but NO codeSource = masquerade (e.g. Godzilla) → no -5
        assertEquals(0, rule.evaluate(
                finding("org.apache.coyote.jsontype.impl.TypeIdResolverBase", null), ctx));
    }

    @Test
    void framework_package_with_empty_codesource_is_not_trusted_zero() {
        assertEquals(0, rule.evaluate(
                finding("org.apache.coyote.ser.PropertyWriter", ""), ctx));
    }

    @Test
    void non_framework_package_is_zero() {
        assertEquals(0, rule.evaluate(finding("com.example.NormalFilter", "file:/opt/app/app.jar"), ctx));
    }

    private Finding finding(String className, String codeSource) {
        Finding f = new Finding();
        f.className = className;
        f.codeSource = codeSource;
        return f;
    }
}
```

- [ ] **Step 2: 跑确认 FAIL**

```
./mvnw.cmd -pl agent -Dtest=WhitelistHitRuleTest test
```
预期：`framework_package_with_null_codesource_is_not_trusted_zero` 与 `framework_package_with_empty_codesource_is_not_trusted_zero` FAIL（现行规则对 null codeSource 仍返回 -5）。

- [ ] **Step 3: 改实现**

把 `WhitelistHitRule.java` 整体替换为：

```java
package com.memhunter.agent.scoring.rules;

import com.memhunter.agent.model.Finding;
import com.memhunter.agent.model.ScanContext;
import com.memhunter.agent.scoring.ScoringRule;

/**
 * Trusts framework classes by lowering their score — but only when there is positive evidence the
 * class really is a framework class: it must have a code source (i.e. it was loaded from a jar).
 *
 * <p>v0.13: a class claiming a trusted framework package (org.apache.coyote.* etc.) but with NO
 * code source is almost certainly a masquerading memshell (e.g. Godzilla renames Jackson classes
 * into org.apache.coyote.* and defines them dynamically, so they have no jar source). Such a class
 * must NOT receive the -5 trust discount. The positive "this is a masquerade" signal is scored
 * separately by {@link MasqueradedPackageRule}.
 */
public class WhitelistHitRule implements ScoringRule {
    public String name() { return "whitelist-hit"; }

    public int evaluate(Finding finding, ScanContext ctx) {
        if (finding == null || ctx == null || ctx.whitelist == null) return 0;
        if (!ctx.whitelist.isFrameworkPackage(finding.className)) return 0;
        // Trust requires a real jar source; a null/empty codeSource means a dynamically-defined
        // class masquerading under a framework package — do not grant the discount.
        if (finding.codeSource == null || finding.codeSource.isEmpty()) return 0;
        return -5;
    }
}
```

- [ ] **Step 4: 跑确认 PASS（4 测试）**

```
./mvnw.cmd -pl agent -Dtest=WhitelistHitRuleTest test
```

- [ ] **Step 5: Commit**

```bash
git add agent/src/main/java/com/memhunter/agent/scoring/rules/WhitelistHitRule.java agent/src/test/java/com/memhunter/agent/scoring/rules/WhitelistHitRuleTest.java
git commit -m "fix(scoring): WhitelistHitRule only trusts framework packages with a real codeSource"
```

---

## Task 2: MasqueradedPackageRule（新规则，TDD）

**Files:**
- Create: `agent/src/main/java/com/memhunter/agent/scoring/rules/MasqueradedPackageRule.java`
- Create: `agent/src/test/java/com/memhunter/agent/scoring/rules/MasqueradedPackageRuleTest.java`

- [ ] **Step 1: 写失败测试**

`MasqueradedPackageRuleTest.java`：

```java
package com.memhunter.agent.scoring.rules;

import com.memhunter.agent.model.Finding;
import com.memhunter.agent.model.ScanContext;
import com.memhunter.agent.scoring.Whitelist;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class MasqueradedPackageRuleTest {
    private final MasqueradedPackageRule rule = new MasqueradedPackageRule();
    private final ScanContext ctx = new ScanContext(null, Whitelist.defaults(), false);

    @Test
    void framework_prefix_with_null_codesource_is_masquerade_plus5() {
        assertEquals(5, rule.evaluate(
                finding("org.apache.coyote.jsontype.impl.TypeIdResolverBase", null), ctx));
    }

    @Test
    void framework_prefix_with_empty_codesource_is_masquerade_plus5() {
        assertEquals(5, rule.evaluate(finding("org.apache.coyote.ser.PropertyWriter", ""), ctx));
    }

    @Test
    void framework_prefix_with_real_codesource_is_zero() {
        assertEquals(0, rule.evaluate(
                finding("org.apache.tomcat.websocket.server.WsFilter",
                        "file:/opt/tomcat/lib/tomcat-websocket.jar"), ctx));
    }

    @Test
    void normal_business_class_with_null_codesource_is_zero() {
        assertEquals(0, rule.evaluate(finding("com.example.UserServlet", null), ctx));
    }

    @Test
    void null_className_is_zero() {
        assertEquals(0, rule.evaluate(finding(null, null), ctx));
    }

    private Finding finding(String className, String codeSource) {
        Finding f = new Finding();
        f.className = className;
        f.codeSource = codeSource;
        return f;
    }
}
```

- [ ] **Step 2: 跑确认 FAIL**

```
./mvnw.cmd -pl agent -Dtest=MasqueradedPackageRuleTest test
```
预期：编译失败（MasqueradedPackageRule 不存在）。

- [ ] **Step 3: 实现**

`MasqueradedPackageRule.java`：

```java
package com.memhunter.agent.scoring.rules;

import com.memhunter.agent.model.Finding;
import com.memhunter.agent.model.ScanContext;
import com.memhunter.agent.scoring.ScoringRule;

/**
 * Flags a class that claims to live under a trusted framework package (org.apache.coyote.* etc.)
 * yet has no code source. A genuine framework class is always loaded from a jar and therefore has
 * a non-empty codeSource; a class with a framework-package name but no jar source is a dynamically
 * defined impostor — a classic memshell evasion (Godzilla renames Jackson classes into
 * org.apache.coyote.* to abuse the framework-package whitelist). Strong, low-false-positive signal.
 */
public class MasqueradedPackageRule implements ScoringRule {
    public String name() { return "masqueraded-package"; }

    public int evaluate(Finding finding, ScanContext ctx) {
        if (finding == null || ctx == null || ctx.whitelist == null) return 0;
        if (!ctx.whitelist.isFrameworkPackage(finding.className)) return 0;
        if (finding.codeSource == null || finding.codeSource.isEmpty()) return 5;
        return 0;
    }
}
```

- [ ] **Step 4: 跑确认 PASS（5 测试）**

```
./mvnw.cmd -pl agent -Dtest=MasqueradedPackageRuleTest test
```

- [ ] **Step 5: Commit**

```bash
git add agent/src/main/java/com/memhunter/agent/scoring/rules/MasqueradedPackageRule.java agent/src/test/java/com/memhunter/agent/scoring/rules/MasqueradedPackageRuleTest.java
git commit -m "feat(scoring): MasqueradedPackageRule flags framework-package names with no jar source"
```

---

## Task 3: 装配 MasqueradedPackageRule + RuleEngine 集成测试（TDD）

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/scoring/RuleEngine.java`
- Modify: `agent/src/test/java/com/memhunter/agent/scoring/RuleEngineTest.java`

- [ ] **Step 1: 写失败集成测试**

在 `RuleEngineTest.java` 的类内（最后一个 `}` 之前）追加两个测试。`import` 若缺 `java.util.ArrayList`/`List`/`com.memhunter.agent.model.Finding`/`ScanContext`/`Whitelist` 则补上（多数已存在，编译报错时按提示加）：

```java
    @Test
    void godzilla_masqueraded_filter_scored_critical() {
        // org.apache.coyote.* prefix + null codeSource + tomcat-filter + /* + dynamic
        // → masquerade(+5), no whitelist discount, plus the usual signals → critical.
        Finding f = new Finding();
        f.type = "tomcat-filter";
        f.className = "org.apache.coyote.jsontype.impl.TypeIdResolverBase";
        f.codeSource = null;
        f.attributes.put("isDynamic", Boolean.TRUE);
        f.attributes.put("urlPatterns", java.util.Arrays.asList("/*"));

        java.util.List<Finding> findings = new java.util.ArrayList<>();
        findings.add(f);
        new RuleEngine().evaluate(findings,
                new com.memhunter.agent.model.ScanContext(
                        null, com.memhunter.agent.scoring.Whitelist.defaults(), false));

        assertEquals("critical", f.level,
                "masqueraded framework-package filter with no jar source must be critical");
    }

    @Test
    void real_framework_class_keeps_whitelist_discount_not_masquerade() {
        // Genuine framework class: framework prefix BUT real jar codeSource → whitelist -5,
        // masquerade rule does NOT fire. Must not be elevated to critical by this change.
        Finding f = new Finding();
        f.type = "tomcat-filter";
        f.className = "org.apache.tomcat.websocket.server.WsFilter";
        f.codeSource = "file:/opt/tomcat/lib/tomcat-websocket.jar";
        f.attributes.put("isDynamic", Boolean.FALSE);

        java.util.List<Finding> findings = new java.util.ArrayList<>();
        findings.add(f);
        new RuleEngine().evaluate(findings,
                new com.memhunter.agent.model.ScanContext(
                        null, com.memhunter.agent.scoring.Whitelist.defaults(), false));

        assertNotEquals("critical", f.level,
                "real framework class must not be scored critical");
    }
```

`assertNotEquals` 需静态导入：在 `RuleEngineTest.java` 文件头确认存在
`import static org.junit.jupiter.api.Assertions.assertNotEquals;`，缺则补上。

- [ ] **Step 2: 跑确认 FAIL**

```
./mvnw.cmd -pl agent -Dtest=RuleEngineTest test
```
预期：`godzilla_masqueraded_filter_scored_critical` FAIL（MasqueradedPackageRule 还没装配进 defaultRules，且 WhitelistHitRule 改动虽已生效，但若未装配 +5 则总分可能不到 10——确认 FAIL 即可）。

- [ ] **Step 3: 装配规则**

在 `RuleEngine.java` 的 `defaultRules()` 的 `Arrays.asList(...)` 里，把末项 `new BenignComponentRule()` 改为带上新规则（注意逗号）：

找到：
```java
                new BaselineNewRule(),
                new BenignComponentRule()
        );
```
替换为：
```java
                new BaselineNewRule(),
                new BenignComponentRule(),
                new MasqueradedPackageRule()
        );
```
（`MasqueradedPackageRule` 与其它规则同包 `com.memhunter.agent.scoring.rules`，`RuleEngine` 已 import 该包的规则，无需额外 import；若编译报找不到符号，在文件头加 `import com.memhunter.agent.scoring.rules.MasqueradedPackageRule;`。）

- [ ] **Step 4: 跑确认 PASS**

```
./mvnw.cmd -pl agent -Dtest=RuleEngineTest test
```

- [ ] **Step 5: 跑 agent 全量，确认无回退**

```
./mvnw.cmd -pl agent test
```
预期：全绿。特别注意 v0.12 的 `RuleEngineTest` 既有用例（benign component → low、agent-bytecode-tampered → critical）仍通过。

- [ ] **Step 6: Commit**

```bash
git add agent/src/main/java/com/memhunter/agent/scoring/RuleEngine.java agent/src/test/java/com/memhunter/agent/scoring/RuleEngineTest.java
git commit -m "feat(scoring): wire MasqueradedPackageRule into RuleEngine; integration tests"
```

---

## Task 4: 设计文档 §25 + 全量构建 + 合并打 tag

**Files:**
- Modify: `java_memshell_scanner_design.md`

- [ ] **Step 1: 追加 §25 v0.13 条目**

找到 `### v0.12.1：误报治理实地复测修复（已完成）` 段落末尾（其后是 `### v1.0：生产可用`）。在 `### v1.0` 之前插入：

```markdown
### v0.13：伪装框架包内存马检测增强（已完成）

实地复扫一台注入哥斯拉（Godzilla）内存马的 Tomcat 9.0.94，发现工具检出了全部哥斯拉马
（3 Filter + 2 Servlet），但伪装包名的几个只到 high/suspicious——哥斯拉把 Jackson 类
伪装成 `org.apache.coyote.*` 包名，滥用受信包白名单（whitelist-hit -5 压低告警）。

核心洞察：真框架类一定从 jar 加载、有 codeSource；伪装类是动态 defineClass 生成的、codeSource=null。
「白名单前缀命中」与「codeSource=null」同时出现即自相矛盾——伪装铁证。

- `WhitelistHitRule` 加 codeSource 可信度门：受信前缀 + codeSource 非空才减 -5；
  codeSource 为空（伪装）则 0
- 新增 `MasqueradedPackageRule`（+5）：受信前缀 + codeSource 空 → 伪装铁证加分
- 两规则均用 `ctx.whitelist.isFrameworkPackage`，逻辑互斥（可信减分 / 伪装加分）
- 零误报：真框架类必有 jar 来源，受信前缀类 + codeSource=null 在正常环境不存在；
  agent-* 型 finding 仍由 RuleEngine 早退保护，不受影响
- 实地验收（哥斯拉 ground truth）：3 个伪装包名 Filter 马（TypeIdResolverBase /
  JsonAppend / PropertyWriter）全部升 critical，数量与哥斯拉客户端 getAllFilter 一致；
  真阳不回退（冰蝎 critical、JSP shell high）、v0.12.1 examples 抑制不回退
- 参考：`docs/superpowers/specs/2026-06-04-v0.13-masquerade-detection-design.md`
```

- [ ] **Step 2: 全量 reactor 构建**

```
./mvnw.cmd -DskipITs verify
```
预期：BUILD SUCCESS，所有模块测试通过。

- [ ] **Step 3: Commit 文档**

```bash
git add java_memshell_scanner_design.md
git commit -m "docs: design doc §25 v0.13 masquerade detection entry"
```

- [ ] **Step 4: Push + merge + tag**

```bash
git push -u origin feature/v0.13-masquerade-detection
git checkout master
git pull --ff-only
git merge --no-ff feature/v0.13-masquerade-detection -m "Merge v0.13: masqueraded framework-package memshell detection"
git push origin master
git tag -a v0.13 -m "v0.13: masqueraded framework-package memshell detection (Godzilla)"
git push origin v0.13
```

- [ ] **Step 5: 验证 CI**

```
sleep 70 && gh run list --limit 2
```
预期：Test + Release 均 success。

---

## Task 5: 实地验收（哥斯拉 ground truth，手动）

> 此任务需用户配合：重启目标 Tomcat（attach 机制：agent 类在目标 JVM 只加载一次，改 agent 代码必须重启目标 JVM 才生效）并重新注入哥斯拉内存马。

- [ ] **Step 1: 重新打包发布 jar**

```
./mvnw.cmd -pl agent,attach -DskipTests package -q
```

- [ ] **Step 2: 请用户重启 Tomcat 9.0.94 + 重新注入哥斯拉，等待确认**

- [ ] **Step 3: 实地复扫**

```
PID=$(jps -l | grep -i bootstrap | awk '{print $1}')
java8 -cp 'attach/target/memhunter-attach.jar;C:\Program Files\Java\jdk1.8.0_181\lib\tools.jar' com.memhunter.attach.AttachMain $PID agent/target/memhunter-agent.jar scan --output 'E:\learn\JavaMemHunter\scan-v013.json'
```
（attach 在 git-bash 下用 java8 系统命令 + tools.jar；输出用绝对路径，否则 attach 端与目标 JVM 工作目录不一致会找不到文件。）

- [ ] **Step 4: 验证结果**

用 `python3 -c "import json; d=json.load(open('scan-v013.json',encoding='utf-8')); ..."`（utf-8 编码）核对：
- 3 个伪装包名 Filter（`org.apache.coyote.*.TypeIdResolverBase` / `JsonAppend` / `PropertyWriter`，带时间戳后缀、urlPattern `/*`）均 level=critical
- 数量与哥斯拉客户端 getAllFilter 显示一致
- 真阳不回退：若该机仍有冰蝎 agent-bytecode-tampered 则 critical；JSP shell 仍 high
- 误报不回退：Tomcat examples 仍 low

- [ ] **Step 5: 清理扫描产物**

```
rm -f scan-v013.json
```

---

## Self-Review 结论

- **Spec 覆盖**：spec §3.1（WhitelistHitRule 门）→ Task 1；§3.2（MasqueradedPackageRule）→ Task 2；§3.3（装配）+ §6.1 集成测试 → Task 3；§6.2 实地验收 → Task 5；§6.3 DoD（文档/构建/合并/tag）→ Task 4。全覆盖。
- **占位符**：无 TBD/TODO；每步含完整代码与命令。
- **类型一致性**：两规则均 `evaluate(Finding, ScanContext)` 返回 int，用 `ctx.whitelist.isFrameworkPackage(finding.className)` + `finding.codeSource` null/empty 判断，前后一致；装配名 `MasqueradedPackageRule` 全程一致。
- **已知陷阱已写明**：现有 `WhitelistHitRuleTest.hits_framework_package`（codeSource=null 期望 -5）会因本改动失效，Task 1 Step 1 整体替换该测试文件解决。
