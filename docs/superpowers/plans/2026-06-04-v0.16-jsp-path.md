# v0.16 JSP webshell 访问路径反推 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ClassScanner 发现 `org.apache.jsp.*_jsp`（JSP webshell）时从类名反推 `.jsp` 访问 URL，使终端摘要显示 `path=[/wwwwxxx.jsp]`。

**Architecture:** agent 端新增纯函数 `JspPathResolver` 反推 URL，ClassScanner 把结果存进 finding.attributes.jspPath；attach 端 ScanSummaryPrinter 对 class-* 型读 jspPath 显示 path=。复用 v0.15 的 appendLocation 机制。

**Tech Stack:** Java 8（agent + attach 两模块），JUnit 5，Jackson，Maven（`./mvnw.cmd`）。

参考 spec：`docs/superpowers/specs/2026-06-04-v0.16-jsp-path-resolution-design.md`

**关键既有事实（已确认）：**
- `ClassScanner.scan()`（agent）循环里构造 finding，line 35 是 `f.id = FindingIdGenerator.generate(...)`，紧接 `findings.add(f)`。finding 字段含 `className`、`attributes`（Map）。
- `ScanSummaryPrinter.appendLocation(StringBuilder line, String type, JsonNode attrs)`（attach）是 v0.15 的 if-else 链，末尾分支是 `else if ("agent-transformer".equals(type))`，之后是方法结束 `}`。已有 import：JsonNode。
- ClassScanner 依赖真实 Instrumentation，不新增单测（spec §4.1）；反推逻辑全在 JspPathResolver 纯函数、显示在 ScanSummaryPrinter，均单测覆盖；ClassScanner 2 行接线靠实地验收。

---

## Task 1: JspPathResolver 纯函数（TDD）

**Files:**
- Create: `agent/src/main/java/com/memhunter/agent/scanner/JspPathResolver.java`
- Create: `agent/src/test/java/com/memhunter/agent/scanner/JspPathResolverTest.java`

- [ ] **Step 1: 写失败测试**

`JspPathResolverTest.java`：
```java
package com.memhunter.agent.scanner;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class JspPathResolverTest {

    @Test
    void root_jsp_maps_to_slash_jsp() {
        assertEquals("/wwwwxxx.jsp", JspPathResolver.toJspUrl("org.apache.jsp.wwwwxxx_jsp"));
        assertEquals("/shell.jsp", JspPathResolver.toJspUrl("org.apache.jsp.shell_jsp"));
    }

    @Test
    void subdir_jsp_maps_package_dots_to_slashes() {
        assertEquals("/admin/x.jsp", JspPathResolver.toJspUrl("org.apache.jsp.admin.x_jsp"));
        assertEquals("/a/b/c.jsp", JspPathResolver.toJspUrl("org.apache.jsp.a.b.c_jsp"));
    }

    @Test
    void non_jsp_class_returns_null() {
        assertNull(JspPathResolver.toJspUrl("com.example.Foo"));
        assertNull(JspPathResolver.toJspUrl("org.apache.coyote.ser.SerializerCache"));
    }

    @Test
    void jsp_prefix_but_not_jsp_suffix_returns_null() {
        assertNull(JspPathResolver.toJspUrl("org.apache.jsp.weird"));
    }

    @Test
    void prefix_only_or_empty_returns_null() {
        assertNull(JspPathResolver.toJspUrl("org.apache.jsp."));
        assertNull(JspPathResolver.toJspUrl("org.apache.jsp._jsp"));
        assertNull(JspPathResolver.toJspUrl(null));
        assertNull(JspPathResolver.toJspUrl(""));
    }
}
```

- [ ] **Step 2: 跑确认 FAIL**

```
./mvnw.cmd -pl agent -Dtest=JspPathResolverTest test
```
预期：编译失败（类不存在）。

- [ ] **Step 3: 实现 JspPathResolver.java**

```java
package com.memhunter.agent.scanner;

/**
 * Reverse-maps a Tomcat-compiled JSP class name to its .jsp access URL.
 *
 * <p>Tomcat compiles {@code /admin/x.jsp} to class {@code org.apache.jsp.admin.x_jsp}. This
 * recovers the URL from the class name so a JSP webshell (a file-based shell, not an in-memory
 * one) shows its access path in the scan summary. Escaped characters in the class name
 * ({@code _002d} etc.) are left as-is — webshell filenames rarely use special characters.
 */
public final class JspPathResolver {

    private JspPathResolver() {}

    private static final String PREFIX = "org.apache.jsp.";
    private static final String SUFFIX = "_jsp";

    /** @return the .jsp URL, or null if className is not a Tomcat-compiled JSP class. */
    public static String toJspUrl(String className) {
        if (className == null || !className.startsWith(PREFIX)) return null;
        String rest = className.substring(PREFIX.length());          // wwwwxxx_jsp / admin.x_jsp
        if (!rest.endsWith(SUFFIX)) return null;
        String body = rest.substring(0, rest.length() - SUFFIX.length()); // wwwwxxx / admin.x
        if (body.isEmpty()) return null;
        return "/" + body.replace('.', '/') + ".jsp";                // /wwwwxxx.jsp / /admin/x.jsp
    }
}
```

- [ ] **Step 4: 跑确认 PASS（5 测试）**

```
./mvnw.cmd -pl agent -Dtest=JspPathResolverTest test
```

- [ ] **Step 5: Commit**

```bash
git add agent/src/main/java/com/memhunter/agent/scanner/JspPathResolver.java agent/src/test/java/com/memhunter/agent/scanner/JspPathResolverTest.java
git commit -m "feat(scanner): JspPathResolver reverse-maps org.apache.jsp.* class names to .jsp URLs"
```

---

## Task 2: ClassScanner 存 jspPath（接线，无单测）

**Files:**
- Modify: `agent/src/main/java/com/memhunter/agent/scanner/ClassScanner.java`

- [ ] **Step 1: 加接线**

在 `ClassScanner.scan()` 循环里，`f.id = FindingIdGenerator.generate(f.type, f.className, "");` 之后、`findings.add(f);` 之前，插入：
```java
            String jspUrl = JspPathResolver.toJspUrl(f.className);
            if (jspUrl != null) f.attributes.put("jspPath", jspUrl);
```
（`JspPathResolver` 与 ClassScanner 同包 `com.memhunter.agent.scanner`，无需 import。`f.attributes` 是已初始化的 Map——确认 Finding.attributes 默认非 null；若为 null 则 finding 模型本就有问题，但现有 scanner 都直接 put，说明默认已初始化。）

- [ ] **Step 2: 编译 + 跑 agent 全量确认无回退**

```
./mvnw.cmd -pl agent test
```
预期：全绿（新增 JspPathResolverTest + 既有全部通过）。

- [ ] **Step 3: Commit**

```bash
git add agent/src/main/java/com/memhunter/agent/scanner/ClassScanner.java
git commit -m "feat(scanner): ClassScanner records jspPath for compiled JSP classes"
```

---

## Task 3: ScanSummaryPrinter 显示 jspPath（TDD）

**Files:**
- Modify: `attach/src/main/java/com/memhunter/attach/ScanSummaryPrinter.java`
- Modify: `attach/src/test/java/com/memhunter/attach/ScanSummaryPrinterTest.java`

- [ ] **Step 1: 写失败测试**

在 `ScanSummaryPrinterTest.java` 追加（`scanLineFor` helper 已在 v0.15 加入）：
```java
    @Test
    void class_servlet_jsp_shows_jspPath_as_path() throws Exception {
        String out = scanLineFor("{\"level\":\"high\",\"type\":\"class-servlet\",\"className\":\"org.apache.jsp.wwwwxxx_jsp\",\"score\":7,"
                + "\"attributes\":{\"jspPath\":\"/wwwwxxx.jsp\"}}");
        assertTrue(out.contains("path=[/wwwwxxx.jsp]"), out);
    }

    @Test
    void class_servlet_without_jspPath_has_no_path_segment() throws Exception {
        String out = scanLineFor("{\"level\":\"high\",\"type\":\"class-servlet\",\"className\":\"com.x.Plain\",\"score\":7,\"attributes\":{}}");
        assertTrue(out.contains("com.x.Plain"), out);
        assertFalse(out.contains("path="), out);
    }

    @Test
    void class_filter_with_jspPath_shows_path() throws Exception {
        String out = scanLineFor("{\"level\":\"high\",\"type\":\"class-filter\",\"className\":\"org.apache.jsp.f_jsp\",\"score\":7,"
                + "\"attributes\":{\"jspPath\":\"/f.jsp\"}}");
        assertTrue(out.contains("path=[/f.jsp]"), out);
    }
```

- [ ] **Step 2: 跑确认 FAIL**

```
./mvnw.cmd -pl attach -Dtest=ScanSummaryPrinterTest test
```
预期：`class_servlet_jsp_shows_jspPath_as_path` / `class_filter_with_jspPath_shows_path` FAIL（当前 class-* 不读 jspPath）。

- [ ] **Step 3: 加 class-* 分支**

在 `ScanSummaryPrinter.appendLocation` 的 if-else 链里，把末尾的 `agent-transformer` 分支之后、方法结束 `}` 之前，新增分支：
```java
        } else if (type != null && type.startsWith("class-")) {
            JsonNode jp = attrs.path("jspPath");
            if (!jp.isMissingNode() && !jp.isNull() && !jp.asText("").isEmpty()) {
                line.append("  path=[").append(jp.asText()).append("]");
            }
        }
```
即把现有：
```java
        } else if ("agent-transformer".equals(type)) {
            JsonNode c = attrs.path("transformerClass");
            if (!c.isMissingNode() && !c.isNull() && !c.asText("").isEmpty()) {
                line.append("  class=").append(c.asText());
            }
        }
    }
```
改为：
```java
        } else if ("agent-transformer".equals(type)) {
            JsonNode c = attrs.path("transformerClass");
            if (!c.isMissingNode() && !c.isNull() && !c.asText("").isEmpty()) {
                line.append("  class=").append(c.asText());
            }
        } else if (type != null && type.startsWith("class-")) {
            JsonNode jp = attrs.path("jspPath");
            if (!jp.isMissingNode() && !jp.isNull() && !jp.asText("").isEmpty()) {
                line.append("  path=[").append(jp.asText()).append("]");
            }
        }
    }
```

- [ ] **Step 4: 跑确认 PASS**

```
./mvnw.cmd -pl attach -Dtest=ScanSummaryPrinterTest test
```

- [ ] **Step 5: 跑 attach 全量确认无回退**

```
./mvnw.cmd -pl attach test
```
预期：全绿。

- [ ] **Step 6: Commit**

```bash
git add attach/src/main/java/com/memhunter/attach/ScanSummaryPrinter.java attach/src/test/java/com/memhunter/attach/ScanSummaryPrinterTest.java
git commit -m "feat(attach): scan summary shows jspPath as access path for compiled JSP classes"
```

---

## Task 4: README + 设计文档 + 全量构建 + 合并打 tag

**Files:**
- Modify: `README.md`
- Modify: `java_memshell_scanner_design.md`

- [ ] **Step 1: README scan 摘要说明补一句**

`README.md` 找到 v0.15 加的那段（提到 filter/servlet 的 path 注解）。在其后补：
```markdown
JSP webshells (compiled to org.apache.jsp.*) show their reverse-mapped .jsp URL, e.g.
path=[/shell.jsp] — a file-based shell to delete from disk, not an in-memory one.
```

- [ ] **Step 2: 设计文档 §25 v0.16 条目**

`java_memshell_scanner_design.md` 找到 `### v0.15：摘要逐条标注访问路径/定位信息（已完成）` 段落末尾（其后是 `### v1.0：生产可用`）。在 `### v1.0` 之前插入：
```markdown
### v0.16：JSP webshell 访问路径反推（已完成）

v0.15 给摘要加了路径，但 JSP webshell（`org.apache.jsp.*_jsp`，经 ClassScanner 发现的
class-servlet）没显示——它经"扫所有已加载类"发现，attributes 无 mappings/urlPatterns。
JSP webshell 是文件型 webshell（磁盘有 .jsp、编译成 org.apache.jsp.*_jsp 类），访问路径
编码在类名里，可纯字符串反推。

- 新增 `JspPathResolver.toJspUrl`（纯函数）：`org.apache.jsp.wwwwxxx_jsp` → `/wwwwxxx.jsp`、
  `org.apache.jsp.admin.x_jsp` → `/admin/x.jsp`；非 JSP 返回 null
- ClassScanner 发现 JSP 类时把反推 URL 存进 finding.attributes.jspPath
- ScanSummaryPrinter 对 class-* 型读 jspPath 显示 `path=[/wwwwxxx.jsp]`
- 不拼磁盘绝对路径（.jsp URL 已足够定位+删文件）、不处理 Tomcat 全套转义规则（YAGNI）
- JSP webshell vs 内存马：JSP 是文件型（磁盘有文件、删文件即清、重启仍在）；内存马无磁盘文件、
  codeSource=null、重启即清——摘要的 path 对前者指向待删文件、对后者指向待封 URL
- 参考：`docs/superpowers/specs/2026-06-04-v0.16-jsp-path-resolution-design.md`
```

- [ ] **Step 3: 全量 reactor 构建**

```
./mvnw.cmd -DskipITs verify
```
预期：BUILD SUCCESS。

- [ ] **Step 4: Commit 文档**

```bash
git add README.md java_memshell_scanner_design.md
git commit -m "docs: v0.16 JSP path resolution — README + design doc §25"
```

- [ ] **Step 5: Push + merge + tag**

```bash
git push -u origin feature/v0.16-jsp-path
git checkout master
git pull --ff-only
git merge --no-ff feature/v0.16-jsp-path -m "Merge v0.16: JSP webshell access-path reverse-resolution"
git push origin master
git tag -a v0.16 -m "v0.16: JSP webshell access-path reverse-resolution"
git push origin v0.16
```

- [ ] **Step 6: 验证 CI**

```
sleep 70 && gh run list --limit 2
```
预期：Test + Release 均 success。

---

## Task 5: 实地验收（手动，用户终端跑）

> 改动涉及 agent（ClassScanner 存 jspPath）——**需重启 Tomcat** 让新 agent 生效（attach 机制：agent 类只加载一次）。

- [ ] **Step 1: 重新打包 agent + attach jar**

```
./mvnw.cmd -pl agent,attach -DskipTests package -q
```

- [ ] **Step 2: 请用户重启 Tomcat + 重新注入哥斯拉（含 JSP shell），等待确认**

- [ ] **Step 3: 扫描验证**

```
PID=$(jps -l | grep -i bootstrap | awk '{print $1}')
java8 -cp 'attach/target/memhunter-attach.jar;C:\Program Files\Java\jdk1.8.0_181\lib\tools.jar' com.memhunter.attach.AttachMain $PID agent/target/memhunter-agent.jar scan
```
验证摘要：
- `org.apache.jsp.wwwwxxx_jsp` 行显示 `path=[/wwwwxxx.jsp]`
- `org.apache.jsp.shell_jsp` 行显示 `path=[/shell.jsp]`
- 哥斯拉内存马 path 显示不回退（v0.15 行为保留）

- [ ] **Step 4: 清理产物**

```
rm -f memhunter-scan-*.json
```

---

## Self-Review 结论

- **Spec 覆盖**：spec §2.1 JspPathResolver → Task 1；§2.2 ClassScanner 接线 → Task 2；§2.3 printer 显示 → Task 3；§4.1 单测（JspPathResolver + ScanSummaryPrinter，ClassScanner 不单测）→ Task 1/3；§4.2 实地 → Task 5；§4.3 DoD → Task 4。全覆盖。
- **占位符**：无 TBD/TODO；每步含完整代码与命令。
- **类型一致性**：`JspPathResolver.toJspUrl(String)→String|null` 在 Task 1 定义、Task 2 调用一致；attributes 键 `jspPath` 在 Task 2 存、Task 3 读一致；printer 分支 `type.startsWith("class-")` 读 `attrs.path("jspPath")` 与存的键名一致。
- **关键点**：Task 2 无单测（spec 已说明理由：ClassScanner 依赖真实 Instrumentation，2 行接线靠实地验收）；Task 5 需重启 Tomcat（涉及 agent 改动）。
